/*
 * File: app/controller/MainMenuController.js
 *
 * This file was generated by Sencha Architect
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Sencha Touch 2.3.x library, under independent license.
 * License of Sencha Architect does not include license for Sencha Touch 2.3.x. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('Recccords.controller.MainMenuController', {
    extend: 'Ext.app.Controller',

    config: {
        refs: {
            viewport: '#viewport'
        },

        control: {
            "button#searchViewButton": {
                tap: 'onSearchTap'
            },
            "button#collectionViewButton": {
                tap: 'onCollectionsTap'
            },
            "button#feedButton": {
                tap: 'onFeedTap'
            },
            "button#wantsViewButton": {
                tap: 'onWantsTap'
            },
            "button#connectDiscogsButton": {
                tap: 'onDiscogsTap'
            }
        }
    },

    onSearchTap: function(button, e, eOpts) {
        searchView = Ext.create('Recccords.view.SearchContainer', {

        });
        var viewport = me.getViewport();
        var main = viewport.down('#mainViewContainer');
        viewport.removeAll(true,false);
        viewport.add(searchView);
    },

    onCollectionsTap: function(button, e, eOpts) {
        collectionView = Ext.create('Recccords.view.CollectionContainer', {
        });
        var viewport = me.getViewport();
        var main = viewport.down('#mainViewContainer');
        viewport.removeAll(true,false);
        viewport.add(collectionView);
    },

    onFeedTap: function(button, e, eOpts) {
        feedView = Ext.create('Recccords.view.FeedContainer', {

        });
        var viewport = me.getViewport();
        var main = viewport.down('#mainViewContainer');
        viewport.removeAll(true,false);
        viewport.add(feedView);
    },

    onWantsTap: function(button, e, eOpts) {
        wantsView = Ext.create('Recccords.view.WantsContainer', {
        });
        var viewport = me.getViewport();
        var main = viewport.down('#mainViewContainer');
        viewport.removeAll(true,false);
        viewport.add(wantsView);
    },

    onDiscogsTap: function(button, e, eOpts) {
        oauthView = Ext.create('Recccords.view.OAuthContainer', {
        });
        var viewport = me.getViewport();
        var main = viewport.down('#mainViewContainer');
        viewport.removeAll(true,false);
        viewport.add(oauthView);
    }

});